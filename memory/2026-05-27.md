# 2026-05-26/27 工作日志 — 主宰面板深度迭代

## 时间
23:00 - 00:31 （跨越至 5月27日）

## 工作内容

### 1. 三个 Bug 修复（23:12-23:25）
- **Bug 1：编辑时间显示 no option** — `timeSel()` 返回完整 `<select>`，编辑表单外层又包一层，嵌套 select。改 `timeOpts()` 只输出 `<option>`，调用方各包 `<select>`。
- **Bug 2：创建/编辑日期时间栏位没对齐** — `.date-field/.time-field` 不是 flex 容器。加 `display:flex;flex-direction:column`。
- **Bug 3：创建后弹窗不自动关闭** — `loadAll()` 失败导致 Promise reject，`.then()` 不执行。加 `.catch()` 关闭弹窗。

### 2. 背景调亮尝试（失败）
- 用户反映看不清，我调背景 #0f0f13→#1a1a22
- 用户批评「变灰模糊，对比度降低」
- 回退到 #0f0f13

### 3. 手机项目页克隆桌面版
- `renderProj()` 重写为 `rDProjects()` 的精确克隆
- 发现巨大 CSS bug：`@media(min-width:768px)` 没有关闭，导致 .proj-detail 及以下所有 CSS（modal/detail-html/markdown/type-buttons）都只在桌面端生效
- 修复：加 `}` 关闭 media query
- 手机卡片从圆点改为左边框样式

### 4. 视觉方案对比
- 创建 `visual-compare.html`，5种方案并排展示（当前/大字版/实色卡片/浅底+加粗/大字+实卡）
- 用户选择方案 D

### 5. 方案 D 实施（最终效果）
- 字号 18→24px（375px手机）
- 文字 #f0f0f5→#fff
- 卡片背景改实色 #1e1e28
- 卡片边框改实色 #2a2a38
- Tab bar 背景 rgba(15,15,19,0.95)→rgba(30,30,40,0.95)
- 弹窗背景 #1a1a20→#1e1e28
- 事件标题字重 500→600
- 次要文字整体提亮一级

## 当前版本
- v0.6.0（方案D + 全文优化）
- 线上：https://wangju8765.github.io/commander-dashboard/
- visual-compare.html 保留在仓库中供参考

## 待办
- GitHub 网络不稳定，偶有 push 超时（需重试）
- 明天看用户反馈，继续调视觉
